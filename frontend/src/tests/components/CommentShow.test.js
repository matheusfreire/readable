import React from 'react';
import {shallow} from 'enzyme';
import CommentShow from '../../components/CommentShow';

describe('<CommentShow />', () => {
    xit('shallow render correctly', () => {
        expect(shallow(<CommentShow />))
    })
})